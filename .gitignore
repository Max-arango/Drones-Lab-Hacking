# local tooling / scratch (repo root)
.claude/
tool-results/
.DS_Store
# MCP server config (per-project, contains the project_ref; the file is
# regenerated by `claude mcp add`, so there's no value committing it).
.mcp.json
# Next.js build output (in case the build runs from the repo root)
/.next/
